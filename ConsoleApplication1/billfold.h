#pragma once
class billfold
{
	//an array of Card*,
	b.a member function add_card(Card*) and
		c.a member function print_cards() that prints info of all cards
		Hint : to call the derived class print you have to downcast the card* to one of the
		derived classes((derivedclass*)card_pointer)
};

